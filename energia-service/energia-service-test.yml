
server:
  port: 9001
  servlet:
    context-path: '/energia-service/'

spring:
  main:
    banner-mode: off
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/energia-test
    username: postgres
    password: Postgres2018!
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        integration:
          envers:
            enabled: false